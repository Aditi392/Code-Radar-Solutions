#include<stdio.h>
#include<stdbool.h>
int main()
{
    int size;
    printf("");
    scanf("%d",&size);
    int arr1[size];
    printf(" ", size);
    for (int i = 0; i < size; i++) {
        scanf("%d", &arr1[i]);{
            for(int j=i+1;j<=size;j++){
                printf("%d",arr[i]);
            }
        }
}
}